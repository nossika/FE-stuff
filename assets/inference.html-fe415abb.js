import{_ as n,p as a,q as s,a1 as e}from"./framework-5866ffd3.js";const t={},o=e(`<h1 id="推理与生成" tabindex="-1"><a class="header-anchor" href="#推理与生成" aria-hidden="true">#</a> 推理与生成</h1><p>训练让模型学会「给定上下文，下一个 token 的概率分布是什么」；**推理（Inference）**则是把这个分布转化为实际文本的过程。同一个模型用不同的采样策略，输出风格与稳定性会有显著差异。</p><hr><h2 id="自回归生成-autoregressive-generation" tabindex="-1"><a class="header-anchor" href="#自回归生成-autoregressive-generation" aria-hidden="true">#</a> 自回归生成（Autoregressive Generation）</h2><p>LLM 的生成是<strong>逐 token 进行</strong>的：每一步把已有序列输入模型，得到下一个 token 在词表上的概率分布，按某种策略采样出一个 token，拼回序列尾部，再进入下一步，直到遇到终止 token（如 <code>&lt;eos&gt;</code>）或达到最大长度。</p><p>这种「<strong>一次只前进一步</strong>」的范式，是 KV Cache（见 <code>transformer.md</code>）能成立的前提，也决定了输出<strong>只能流式产出</strong>——服务端通常用 SSE 把每个新 token 推给前端，UI 才能呈现「打字机」效果。</p><hr><h2 id="采样策略-sampling" tabindex="-1"><a class="header-anchor" href="#采样策略-sampling" aria-hidden="true">#</a> 采样策略（Sampling）</h2><p>在每一步，模型会输出一个 logits 向量（词表大小），经 softmax 得到概率分布。怎么从这个分布选下一个 token，就是采样策略要解决的问题。</p><h3 id="temperature-温度" tabindex="-1"><a class="header-anchor" href="#temperature-温度" aria-hidden="true">#</a> Temperature（温度）</h3><p>在 softmax 前对 logits 除以一个温度系数 T：</p><ul><li><strong>T → 0</strong>：分布趋于尖锐，几乎每次都选最高概率的 token，输出<strong>确定、保守</strong>。</li><li><strong>T = 1</strong>：使用模型原始分布。</li><li><strong>T &gt; 1</strong>：分布趋于平坦，低概率 token 也有机会被选中，输出<strong>多样、发散</strong>，但更易跑偏。</li></ul><blockquote><p>Temperature 不改变排名，只改变概率差距，因此通常和下面的截断策略组合使用。</p></blockquote><h3 id="greedy-decoding-贪心" tabindex="-1"><a class="header-anchor" href="#greedy-decoding-贪心" aria-hidden="true">#</a> Greedy Decoding（贪心）</h3><p>每步直接选概率最高的 token。等价于 T → 0。优点是<strong>完全确定</strong>、可复现；缺点是<strong>容易陷入重复</strong>（&quot;The cat sat on the cat sat on the…&quot;），且在创造性任务上表现单一。</p><h3 id="top-k-采样" tabindex="-1"><a class="header-anchor" href="#top-k-采样" aria-hidden="true">#</a> Top-k 采样</h3><p>只从概率最高的 k 个 token 中按归一化后的概率采样。k 越小越保守，越大越接近原分布。问题是 k 是固定值：当分布很尖锐时 k 个里包含了大量低概率 token，分布很平坦时又可能裁掉合理候选。</p><h3 id="top-p-nucleus-采样" tabindex="-1"><a class="header-anchor" href="#top-p-nucleus-采样" aria-hidden="true">#</a> Top-p / Nucleus 采样</h3><p>按概率从高到低累加，取<strong>累计概率刚好达到 p</strong> 的最小集合，再在这个集合里采样。相比 Top-k，它的候选数会随分布形状<strong>自适应</strong>：分布尖锐时只剩少数几个，分布平坦时自动放宽。这是当前最常用的策略，常见组合是 <code>temperature=0.7, top_p=0.9</code>。</p><h3 id="重复惩罚" tabindex="-1"><a class="header-anchor" href="#重复惩罚" aria-hidden="true">#</a> 重复惩罚</h3><p>为避免循环输出，常见参数：</p><ul><li><strong>frequency_penalty</strong>：根据 token 在已生成序列中出现的频次降低其概率。</li><li><strong>presence_penalty</strong>：只要 token 出现过就施加固定惩罚，鼓励引入新词。</li></ul><h3 id="beam-search" tabindex="-1"><a class="header-anchor" href="#beam-search" aria-hidden="true">#</a> Beam Search</h3><p>并行维护若干条候选序列（beam），每步基于整段序列的累积概率挑出 Top-N 个分支继续展开，最后选总分最高的一条。优点是能找到全局更优的序列，常用于翻译、摘要等<strong>确定性任务</strong>。缺点是在开放式生成中容易给出过于「平均」、缺乏新意的文本，因此现代对话模型更偏好采样而非 Beam。</p><hr><h2 id="结构化输出" tabindex="-1"><a class="header-anchor" href="#结构化输出" aria-hidden="true">#</a> 结构化输出</h2><p>让模型生成自然语言只需文本接龙，但要让模型可靠地输出<strong>机器可解析</strong>的格式（JSON、SQL、特定 DSL），需要额外手段。</p><h3 id="json-mode" tabindex="-1"><a class="header-anchor" href="#json-mode" aria-hidden="true">#</a> JSON Mode</h3><p>许多 API 提供「JSON Mode」开关，强制输出合法 JSON。原理上通常是：在系统提示里加约束 + 在解码时校验输出可解析。<strong>注意它只保证「是合法 JSON」，不保证字段符合你想要的 schema</strong>，业务侧仍要做校验。</p><h3 id="constrained-decoding-约束解码" tabindex="-1"><a class="header-anchor" href="#constrained-decoding-约束解码" aria-hidden="true">#</a> Constrained Decoding（约束解码）</h3><p>更强的方案是在每一步采样前，根据语法（JSON Schema、正则、CFG）<strong>屏蔽掉不合法的 token</strong>——只在合法候选里采样，从根本上保证输出符合结构。代表实现有 <code>outlines</code>、<code>xgrammar</code>、llama.cpp 的 GBNF 语法等。</p><blockquote><p>这与 <code>llm.md</code> 中的 <strong>Function Call</strong> 是一体两面：Function Call 是上层约定（声明工具及参数 schema），约束解码是下层实现手段之一，确保模型选出的工具名与参数确实落在合法集合里。</p></blockquote><hr><h2 id="流式输出-streaming" tabindex="-1"><a class="header-anchor" href="#流式输出-streaming" aria-hidden="true">#</a> 流式输出（Streaming）</h2><h3 id="协议层" tabindex="-1"><a class="header-anchor" href="#协议层" aria-hidden="true">#</a> 协议层</h3><p>服务端最常用 <strong>SSE（Server-Sent Events）</strong>：单向、基于 HTTP，天然适合「每生成一个 token 推一次」的场景。OpenAI 兼容协议中每条消息形如 <code>data: {&quot;choices&quot;:[{&quot;delta&quot;:{&quot;content&quot;:&quot;x&quot;}}]}</code>，最后以 <code>data: [DONE]</code> 结束。少数场景（双向、多媒体）也会用 WebSocket。</p><h3 id="前端实现" tabindex="-1"><a class="header-anchor" href="#前端实现" aria-hidden="true">#</a> 前端实现</h3><p>浏览器原生 <code>EventSource</code> 不支持自定义 header（如鉴权），生产代码通常用 <code>fetch</code> + <code>ReadableStream</code> 自行解析：</p><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">const</span> res <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">fetch</span><span class="token punctuation">(</span>url<span class="token punctuation">,</span> <span class="token punctuation">{</span> method<span class="token operator">:</span> <span class="token string">&#39;POST&#39;</span><span class="token punctuation">,</span> headers<span class="token punctuation">,</span> body <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">const</span> reader <span class="token operator">=</span> res<span class="token punctuation">.</span>body<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">getReader</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">const</span> decoder <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">TextDecoder</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">let</span> buffer <span class="token operator">=</span> <span class="token string">&#39;&#39;</span><span class="token punctuation">;</span>
<span class="token keyword">while</span> <span class="token punctuation">(</span><span class="token boolean">true</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> <span class="token punctuation">{</span> value<span class="token punctuation">,</span> done <span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token keyword">await</span> reader<span class="token punctuation">.</span><span class="token function">read</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">if</span> <span class="token punctuation">(</span>done<span class="token punctuation">)</span> <span class="token keyword">break</span><span class="token punctuation">;</span>
  buffer <span class="token operator">+=</span> decoder<span class="token punctuation">.</span><span class="token function">decode</span><span class="token punctuation">(</span>value<span class="token punctuation">,</span> <span class="token punctuation">{</span> stream<span class="token operator">:</span> <span class="token boolean">true</span> <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">const</span> line <span class="token keyword">of</span> buffer<span class="token punctuation">.</span><span class="token function">split</span><span class="token punctuation">(</span><span class="token string">&#39;\\n\\n&#39;</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>line<span class="token punctuation">.</span><span class="token function">startsWith</span><span class="token punctuation">(</span><span class="token string">&#39;data: &#39;</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">const</span> payload <span class="token operator">=</span> line<span class="token punctuation">.</span><span class="token function">slice</span><span class="token punctuation">(</span><span class="token number">6</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">if</span> <span class="token punctuation">(</span>payload <span class="token operator">===</span> <span class="token string">&#39;[DONE]&#39;</span><span class="token punctuation">)</span> <span class="token keyword">return</span><span class="token punctuation">;</span>
      <span class="token keyword">const</span> delta <span class="token operator">=</span> <span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">parse</span><span class="token punctuation">(</span>payload<span class="token punctuation">)</span><span class="token punctuation">.</span>choices<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">.</span>delta<span class="token punctuation">.</span>content <span class="token operator">??</span> <span class="token string">&#39;&#39;</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="工程注意" tabindex="-1"><a class="header-anchor" href="#工程注意" aria-hidden="true">#</a> 工程注意</h3><ul><li><strong>TTFT（Time To First Token）</strong> 与 <strong>TPOT（Time Per Output Token）</strong> 是流式体验的两个核心指标，长上下文会显著拉高 TTFT。</li><li><strong>中断（AbortController）</strong> 必须支持：用户停止后要立即关闭连接，避免后端继续生成产生费用。</li><li><strong>Markdown 增量渲染</strong>：每来一个 token 都重新解析整段会卡顿；常见做法是节流 + 渲染稳定前缀，或使用支持流式解析的 Markdown 库。</li></ul><hr><h2 id="上下文窗口-context-window" tabindex="-1"><a class="header-anchor" href="#上下文窗口-context-window" aria-hidden="true">#</a> 上下文窗口（Context Window）</h2><p><strong>上下文窗口</strong>指模型一次前向能处理的最大 token 数（输入 + 输出）。它由训练时的位置编码范围与显存共同决定，是推理阶段绕不开的硬约束。</p><h3 id="为什么有限制" tabindex="-1"><a class="header-anchor" href="#为什么有限制" aria-hidden="true">#</a> 为什么有限制</h3><p>标准自注意力的计算量与显存占用都是 <strong>O(n²)</strong>（n 为序列长度）：序列翻倍，注意力矩阵面积是 4 倍。即便有 KV Cache 缓解推理冗余，长上下文仍意味着显存与延迟同步上升。</p><h3 id="常见扩展手段" tabindex="-1"><a class="header-anchor" href="#常见扩展手段" aria-hidden="true">#</a> 常见扩展手段</h3><ul><li><strong>位置插值 / NTK / YaRN</strong>：在 RoPE 基础上调整频率，使模型在未训练过的长度上仍能工作（位置编码细节见 <code>tokenizer.md</code>）。</li><li><strong>稀疏 / 局部注意力</strong>：只让每个 token 关注一个窗口或固定模式（Sliding Window、Longformer 等），把复杂度降到接近 O(n)。</li><li><strong>分层检索（RAG）</strong>：把长文档先用检索召回，再喂给短上下文模型，回避「真长上下文」的成本（见 <code>llm.md</code>）。</li></ul><h3 id="工程注意-1" tabindex="-1"><a class="header-anchor" href="#工程注意-1" aria-hidden="true">#</a> 工程注意</h3><ul><li>「128K 窗口」并不等于「在 128K 中表现一致」，常见有<strong>中间遗忘（Lost in the Middle）</strong>：放在上下文中段的信息更容易被忽略。</li><li>输入越长，<strong>首 token 延迟（TTFT）</strong> 越高，前端在做流式 UI 时尤其要关注；可以通过 prompt 压缩、KV Cache 复用（前缀缓存）等方式优化。</li></ul><hr><h2 id="推理优化" tabindex="-1"><a class="header-anchor" href="#推理优化" aria-hidden="true">#</a> 推理优化</h2><p>模型实际上线时还有一系列工程层面的优化，前端虽然不直接实现，但理解概念有助于和后端协作。</p><ul><li><strong>KV Cache 复用 / 前缀缓存</strong>：相同的 system prompt 在多次请求间复用 KV，可大幅降低 TTFT 与成本。</li><li><strong>Continuous Batching</strong>：把多个用户请求动态拼到同一个 batch 里推理，提升 GPU 利用率（vLLM、TGI 等推理框架的核心能力）。</li><li><strong>投机采样（Speculative Decoding）</strong>：用一个小模型先草拟若干 token，大模型一次性验证，验证通过的部分直接接受，从而把「逐 token 串行」部分变成并行，加速 1.5～3 倍。</li><li><strong>量化（Quantization）</strong>：把权重从 FP16 压到 INT8 / INT4，显存与带宽同步下降，是端侧、消费级显卡部署的必经之路。</li></ul>`,54),p=[o];function c(r,i){return a(),s("div",null,p)}const d=n(t,[["render",c],["__file","inference.html.vue"]]);export{d as default};
