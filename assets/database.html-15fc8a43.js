import{_ as n,p as s,q as e,a1 as a}from"./framework-5866ffd3.js";const t={},i=a(`<h1 id="数据库" tabindex="-1"><a class="header-anchor" href="#数据库" aria-hidden="true">#</a> 数据库</h1><h2 id="概览" tabindex="-1"><a class="header-anchor" href="#概览" aria-hidden="true">#</a> 概览</h2><table><thead><tr><th>类型</th><th>代表</th><th>一句话</th></tr></thead><tbody><tr><td>关系型</td><td>MySQL</td><td>表 + SQL，强一致、事务成熟，Web 业务常用</td></tr><tr><td>关系型（对象-关系）</td><td>PostgreSQL</td><td>功能强大的 SQL，JSON / GIS / 扩展丰富</td></tr><tr><td>键值 / 内存</td><td>Redis</td><td>内存为主，极快读写，丰富数据结构</td></tr><tr><td>文档型</td><td>MongoDB</td><td>JSON 式文档，灵活 schema，水平扩展友好</td></tr><tr><td>搜索 / 倒排索引</td><td>Elasticsearch</td><td>全文检索 + 日志分析，近实时、聚合强</td></tr><tr><td>列式 OLAP</td><td>ClickHouse</td><td>列式存储 + 向量化，海量数据聚合查询性能极强</td></tr></tbody></table><p>下文按「简介 → 适用场景 → 语法 / 机制」展开，便于对照选型。</p><hr><h2 id="mysql" tabindex="-1"><a class="header-anchor" href="#mysql" aria-hidden="true">#</a> MySQL</h2><h3 id="简介" tabindex="-1"><a class="header-anchor" href="#简介" aria-hidden="true">#</a> 简介</h3><p>开源关系型数据库（RDBMS），用<strong>表、行、列</strong>建模，通过 <strong>SQL</strong> 访问。支持事务（ACID）、索引、主从复制等，是 Web 业务系统最常用的<strong>主库</strong>方案之一。</p><h3 id="适用场景" tabindex="-1"><a class="header-anchor" href="#适用场景" aria-hidden="true">#</a> 适用场景</h3><ul><li><strong>核心业务数据</strong>：订单、用户、账务等需要强一致与复杂查询的场景。</li><li><strong>多表关联与报表</strong>：JOIN、聚合、窗口函数等 SQL 能力成熟。</li><li><strong>事务要求高</strong>：转账、库存扣减等需要 InnoDB 事务保证。</li><li><strong>团队以 SQL 为主</strong>：生态、工具、运维经验都较统一。</li></ul><p>不太适合：单表数据量极大且难以分库分表时，需配合分片中间件或考虑分布式数据库；以非结构化数据为主、schema 频繁变动时，文档库更顺手。</p><h3 id="语法要点" tabindex="-1"><a class="header-anchor" href="#语法要点" aria-hidden="true">#</a> 语法要点</h3><p>建模层次：<strong>库（database）→ 表（table）→ 行 / 列</strong>。典型操作：<code>CREATE DATABASE</code> / <code>CREATE TABLE</code>、<code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code>、<code>SELECT</code>，以及索引、约束、事务控制（<code>BEGIN</code> / <code>COMMIT</code> / <code>ROLLBACK</code>）等。</p><h3 id="分库分表" tabindex="-1"><a class="header-anchor" href="#分库分表" aria-hidden="true">#</a> 分库分表</h3><p>单实例 MySQL 在存储容量、连接数、写 QPS 上都有上限，单表行数过大也会让 B+ 树变深、索引效率下降。<strong>分库分表</strong>是把数据按某种规则拆到多个库 / 多张表上，以突破单机瓶颈。</p><p>按维度分为：</p><ul><li><strong>垂直拆分</strong>（按字段 / 业务） <ul><li><strong>垂直分表</strong>：把一张大表按字段拆小，冷热字段或大字段（如 <code>TEXT</code> / <code>BLOB</code>）单独成表，减小主表行宽。</li><li><strong>垂直分库</strong>：按业务域拆库，如用户库、订单库、商品库，降低单库压力、便于独立部署。</li></ul></li><li><strong>水平拆分</strong>（按行 / 数据量） <ul><li><strong>水平分表</strong>：多张表结构相同，按路由键把行分散进去，如 <code>order_0 … order_15</code>。</li><li><strong>水平分库</strong>：把分表再分散到多个库 / 多台实例上，真正扩展写入能力。实际方案常水平分库 + 水平分表一起做。</li></ul></li></ul><p>常见<strong>路由策略</strong>：</p><ul><li><strong>Hash 取模</strong>：<code>shard = hash(key) % N</code>，分布均匀，但扩容需大规模迁移。</li><li><strong>范围分片</strong>：按时间 / ID 区间切，按时间归档方便，但易出现热点分片。</li><li><strong>一致性哈希</strong>：扩容只影响相邻分片，适合分片数动态变化。</li><li><strong>路由表 / 基因法</strong>：用映射表或把分片键&quot;基因&quot;编码进主键，支持非分片键查询。</li></ul><p><strong>常见中间件 / 方案</strong>：ShardingSphere（JDBC / Proxy 两种形态）、Vitess（YouTube 开源，K8s 友好）、云厂商分布式 DB（TiDB、PolarDB-X、OceanBase 等，通常无需手工分片）。</p><p><strong>带来的复杂度</strong>（选型时要权衡）：</p><ul><li><strong>跨库 JOIN / 聚合困难</strong>：通常靠冗余字段、在应用层聚合或宽表预计算绕开。</li><li><strong>分布式事务</strong>：跨分片写要用 XA、TCC、Seata 或最终一致性方案，性能和复杂度都上去。</li><li><strong>全局唯一 ID</strong>：不能再依赖自增主键，改用雪花算法（Snowflake）、号段模式、UUID 等。</li><li><strong>跨分片排序 / 分页</strong>：<code>ORDER BY ... LIMIT</code> 需从各分片拉数据归并，深分页代价高。</li><li><strong>扩容与再分片</strong>：数据迁移、双写、切流量，工程量大；选路由策略时就要提前考虑。</li></ul><blockquote><p>实践建议：单表在千万到亿级、或写入成为瓶颈时再考虑分库分表；能用读写分离 + 归档 + 索引优化解决的，优先做这些。若业务已经足够大、且团队有运维能力，直接上 TiDB / OceanBase 等分布式数据库，比自建分片中间件更省心。</p></blockquote><h3 id="binlog" tabindex="-1"><a class="header-anchor" href="#binlog" aria-hidden="true">#</a> Binlog</h3><p><strong>Binary Log</strong>，MySQL Server 层的逻辑日志，按时间顺序记录所有修改数据的操作（DDL + DML，不含 <code>SELECT</code>），所有存储引擎通用。注意与 InnoDB 的 <strong>redo log</strong> 区分：redo log 是引擎层的物理日志，只服务于<strong>崩溃恢复</strong>；binlog 面向外部消费，是复制与数据同步的基础设施。</p><p>主要用途：</p><ul><li><strong>主从复制</strong>：从库拉取主库 binlog 重放，实现读写分离、高可用、跨机房同步。</li><li><strong>时间点恢复（PITR）</strong>：<code>全量备份 + 之后的 binlog 重放</code> 恢复到任意时刻，比单靠全量备份精细得多。</li><li><strong>数据订阅 / CDC</strong>：Canal、Debezium、Maxwell 等工具伪装成从库订阅 binlog，把变更实时同步到 Kafka / ES / ClickHouse / Redis，是<strong>缓存更新、搜索同步、数仓接入</strong>的标准做法。</li></ul><hr><h2 id="postgresql" tabindex="-1"><a class="header-anchor" href="#postgresql" aria-hidden="true">#</a> PostgreSQL</h2><h3 id="简介-1" tabindex="-1"><a class="header-anchor" href="#简介-1" aria-hidden="true">#</a> 简介</h3><p>开源<strong>对象-关系型</strong>数据库（ORDBMS），常简称 <strong>pgsql / Postgres</strong>。在标准 SQL 之上扩展了大量能力：<strong>JSONB</strong>（原生 JSON 列与索引）、<strong>数组 / 自定义类型 / 枚举</strong>、<strong>窗口函数 / CTE / 递归查询</strong>、<strong>全文检索</strong>、<strong>PostGIS 地理空间</strong>，并通过扩展机制（<code>CREATE EXTENSION</code>）接入向量检索（pgvector）、时序（TimescaleDB）等。</p><p>与 MySQL 的主要差异：</p><ul><li><strong>SQL 特性更完整</strong>：对标准 SQL 支持更全面，复杂查询 / 分析能力更强。</li><li><strong>事务与一致性</strong>：基于 MVCC 实现多版本并发控制，DDL 也可放入事务中回滚。</li><li><strong>JSONB</strong>：既保留关系模型，又能像文档库一样存储 / 查询半结构化数据。</li><li><strong>扩展生态</strong>：PostGIS、pgvector、TimescaleDB 等让一库承担多种职责。</li><li><strong>复制 / 高可用</strong>：原生流复制 + 逻辑复制，社区高可用方案成熟（Patroni 等）。</li></ul><h3 id="适用场景-1" tabindex="-1"><a class="header-anchor" href="#适用场景-1" aria-hidden="true">#</a> 适用场景</h3><ul><li><strong>复杂业务与分析混合</strong>：报表、BI、复杂 JOIN / 窗口函数较多时体验优于 MySQL。</li><li><strong>半结构化字段</strong>：既要关系表又要灵活字段时，用 JSONB 一张表搞定，避免引入额外文档库。</li><li><strong>地理 / 位置服务</strong>：PostGIS 几乎是行业标准。</li><li><strong>AI / 向量检索</strong>：用 pgvector 作为嵌入向量存储与近邻检索。</li><li><strong>时序与 IoT</strong>：搭配 TimescaleDB 处理大规模时序数据。</li><li><strong>对数据一致性与 SQL 表达力要求高的中后台系统</strong>。</li></ul><p>不太适合：极高写入吞吐、海量简单 KV 读写（Redis / 专用 KV 更合适）；需要大规模水平扩展时，要借助 Citus 等分片方案或改用分布式数据库。</p><h3 id="语法要点-1" tabindex="-1"><a class="header-anchor" href="#语法要点-1" aria-hidden="true">#</a> 语法要点</h3><p>整体 SQL 与 MySQL 接近，常见差异：</p><ul><li>官方客户端是 <code>psql</code>：<code>\\l</code> 列库、<code>\\c dbname</code> 切库、<code>\\dt</code> 列表。</li><li>自增主键使用 <code>SERIAL</code> / <code>BIGSERIAL</code> 或 <code>GENERATED AS IDENTITY</code>，没有 <code>AUTO_INCREMENT</code>。</li><li>字符串字面量用单引号 <code>&#39;...&#39;</code>；未加引号的标识符会被折叠为小写，需保留大小写时用双引号 <code>&quot;...&quot;</code>。</li><li>JSONB 查询示例：</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>-- 建表：一列关系字段 + 一列 JSONB
CREATE TABLE events (
  id BIGSERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL,
  payload JSONB NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 取 JSON 字段：-&gt; 返回 jsonb，-&gt;&gt; 返回 text
SELECT payload-&gt;&#39;device&#39;-&gt;&gt;&#39;os&#39; AS os FROM events WHERE user_id = 1;

-- 条件过滤 + JSONB 索引
CREATE INDEX idx_events_payload ON events USING GIN (payload);
SELECT * FROM events WHERE payload @&gt; &#39;{&quot;level&quot;:&quot;error&quot;}&#39;;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><hr><h2 id="redis" tabindex="-1"><a class="header-anchor" href="#redis" aria-hidden="true">#</a> Redis</h2><h3 id="简介-2" tabindex="-1"><a class="header-anchor" href="#简介-2" aria-hidden="true">#</a> 简介</h3><p>开源<strong>内存</strong>键值存储，常用于缓存、会话、分布式锁、轻量消息队列等。值支持多种数据结构（字符串、哈希、列表、集合、有序集合等），读写延迟极低。持久化提供两种机制：<strong>RDB</strong>（定时快照）与 <strong>AOF</strong>（追加写操作日志），可单独或组合使用，在启动恢复速度与数据丢失窗口之间取舍。</p><h3 id="适用场景-2" tabindex="-1"><a class="header-anchor" href="#适用场景-2" aria-hidden="true">#</a> 适用场景</h3><ul><li><strong>缓存</strong>：热点数据、页面片段、接口结果，减轻 MySQL 等后端压力。</li><li><strong>会话 / 限流 / 分布式锁</strong>：短期键、<code>EXPIRE</code>、<code>SET NX</code> 等模式。</li><li><strong>排行榜与计数</strong>：有序集合（<code>ZADD</code> / <code>ZRANGE</code>）、原子自增。</li><li><strong>简单消息与任务队列</strong>：<code>List</code> 作队列、<code>Stream</code> 作带消费组的日志流、<code>Pub/Sub</code> 作即时广播（见下文）。</li></ul><p>不太适合：大体积冷数据全量放内存成本高；需要复杂即席分析查询时不如 SQL 或专用分析引擎；强一致跨键事务不是 Redis 强项。</p><h3 id="语法要点-2" tabindex="-1"><a class="header-anchor" href="#语法要点-2" aria-hidden="true">#</a> 语法要点</h3><p>通过 <code>redis-cli</code> 或各语言客户端连接后直接操作键值，命令按数据结构分类（大小写不敏感）。常用命令：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># 通用：过期 / 查找 / 删除
SET key val [EX seconds] [NX]     # 写入，NX 只在不存在时写（常用于分布式锁）
GET key                           # 读
DEL key                           # 删除
EXISTS key                        # 是否存在
EXPIRE key seconds                # 设置过期时间
TTL key                           # 查询剩余有效期

# Hash（哈希）：适合对象字段
HSET key f1 v1 f2 v2              # 批量设置字段
HGET key f1                       # 取某个字段
HGETALL key                       # 取全部字段

# List（列表）：双端队列，适合轻量队列
LPUSH key v1 v2                   # 左侧入队（RPUSH 从右侧）
LPOP key                          # 左侧出队（RPOP 从右侧）
LRANGE key start stop             # 按索引范围读取

# Set（集合）：自动去重
SADD key v1 v2                    # 添加成员
SMEMBERS key                      # 列出全部成员
SINTER key1 key2                  # 交集（并集 SUNION、差集 SDIFF）

# Sorted Set（有序集合）：带分值排序，适合排行榜
ZADD key score1 m1 score2 m2      # 添加 / 更新成员分值
ZRANGE key start stop [WITHSCORES]      # 按排名区间取
ZRANGEBYSCORE key min max               # 按分值区间取
ZRANK key m1                            # 查询成员排名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>事务通过 <code>MULTI / EXEC</code> 将多条命令打包执行（不支持回滚，且执行期间不会被其他命令打断）；更复杂的原子逻辑一般用 <strong>Lua 脚本</strong>（<code>EVAL</code>）。</p><h3 id="发布-订阅-pub-sub" tabindex="-1"><a class="header-anchor" href="#发布-订阅-pub-sub" aria-hidden="true">#</a> 发布 / 订阅（Pub/Sub）</h3><p>客户端 <code>SUBSCRIBE channel</code> 订阅频道，其他客户端 <code>PUBLISH channel message</code> 推送消息。<strong>消息不持久化</strong>：订阅前或断连期间产生的消息不会补发；需要可靠投递时改用 Stream、List 或专门的消息队列。</p><hr><h2 id="mongodb" tabindex="-1"><a class="header-anchor" href="#mongodb" aria-hidden="true">#</a> MongoDB</h2><h3 id="简介-3" tabindex="-1"><a class="header-anchor" href="#简介-3" aria-hidden="true">#</a> 简介</h3><p>开源<strong>文档型</strong>数据库，数据以 <strong>BSON/类 JSON 文档</strong>为单位存储在<strong>集合</strong>中，无固定表结构要求（灵活 schema）。默认数据<strong>持久化到磁盘</strong>；也可选用内存引擎等部署形态。支持副本集、分片集群，适合数据模型嵌套多、字段变化频繁的场景。</p><h3 id="适用场景-3" tabindex="-1"><a class="header-anchor" href="#适用场景-3" aria-hidden="true">#</a> 适用场景</h3><ul><li><strong>内容 / 目录 / 配置</strong>：文档结构差异大，用嵌套文档比多表 JOIN 更自然。</li><li><strong>物联网、日志、事件流</strong>：高写入、按时间或设备分片扩展。</li><li><strong>快速迭代产品</strong>：schema 频繁调整，减少迁表成本（仍需索引与数据治理）。</li><li><strong>地理空间、聚合管道</strong>：内置 <code>$geoNear</code>、聚合管道等能力。</li></ul><p>不太适合：重度依赖多文档 ACID 事务的业务（4.0+ 已支持多文档事务，但性能与运维成本高于关系库）；团队只熟悉 SQL 且报表均为关系模型时，MySQL / PostgreSQL 更省事。</p><h3 id="语法要点-3" tabindex="-1"><a class="header-anchor" href="#语法要点-3" aria-hidden="true">#</a> 语法要点</h3><p>建模层次：<strong>库（database）→ 集合（collection）→ 文档（document）</strong>。操作通过 <code>mongosh</code> 或各语言驱动以<strong>方法调用</strong>而非 SQL 表达，例如 <code>db.users.insertOne({...})</code>、<code>db.users.find({ age: { $gt: 18 } })</code>、<code>db.users.updateOne(...)</code>、聚合用 <code>db.coll.aggregate([...])</code>。索引通过 <code>createIndex</code> 创建，支持复合索引、TTL、地理索引等。</p><hr><h2 id="elasticsearch" tabindex="-1"><a class="header-anchor" href="#elasticsearch" aria-hidden="true">#</a> Elasticsearch</h2><h3 id="简介-4" tabindex="-1"><a class="header-anchor" href="#简介-4" aria-hidden="true">#</a> 简介</h3><p>基于 <strong>Lucene</strong> 的开源分布式<strong>搜索引擎</strong>，常简称 <strong>ES</strong>。核心能力是<strong>倒排索引</strong>下的全文检索与打分排序，同时提供丰富的聚合（aggregation）能力，常用于日志分析与搜索场景。数据以 <strong>JSON 文档</strong>形式写入，按 <strong>index（索引）→ shard（分片）→ document（文档）</strong> 组织，天然分布式、近实时（写入到可检索通常在秒级）。</p><p>周边常称 <strong>ELK / Elastic Stack</strong>：<strong>Elasticsearch（存储 + 检索）+ Logstash / Beats（采集）+ Kibana（可视化）</strong>。</p><h3 id="适用场景-4" tabindex="-1"><a class="header-anchor" href="#适用场景-4" aria-hidden="true">#</a> 适用场景</h3><ul><li><strong>全文检索</strong>：商品 / 内容 / 文档搜索，支持分词、相关性打分、高亮、拼写纠错、同义词等。</li><li><strong>日志与可观测性</strong>：应用日志、Nginx 日志、审计日志集中检索与分析（ELK 经典组合）。</li><li><strong>聚合分析 / 仪表盘</strong>：按维度分桶、时间序列聚合，配合 Kibana 做运营 / 运维大盘。</li><li><strong>多条件复杂查询</strong>：布尔组合、范围、地理位置（geo）、嵌套文档等。</li><li><strong>向量检索</strong>：较新版本支持 kNN 近邻检索，可用于语义搜索。</li></ul><p>不太适合：作为业务主库（非事务型，不建议作为唯一数据源）；频繁更新同一文档的重写场景（成本较高）；强一致的跨文档事务。</p><h3 id="语法要点-4" tabindex="-1"><a class="header-anchor" href="#语法要点-4" aria-hidden="true">#</a> 语法要点</h3><p>通过 <strong>HTTP + JSON</strong> 的 REST API 访问，Kibana 里常用 <strong>Dev Tools</strong> 直接写请求。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># 建索引（含字段映射）
PUT /articles
{
  &quot;mappings&quot;: {
    &quot;properties&quot;: {
      &quot;title&quot;:   { &quot;type&quot;: &quot;text&quot; },
      &quot;tags&quot;:    { &quot;type&quot;: &quot;keyword&quot; },
      &quot;views&quot;:   { &quot;type&quot;: &quot;integer&quot; },
      &quot;created&quot;: { &quot;type&quot;: &quot;date&quot; }
    }
  }
}

# 写入文档
POST /articles/_doc
{ &quot;title&quot;: &quot;Elasticsearch 入门&quot;, &quot;tags&quot;: [&quot;search&quot;,&quot;log&quot;], &quot;views&quot;: 10, &quot;created&quot;: &quot;2025-01-01&quot; }

# 全文检索 + 过滤 + 分页
GET /articles/_search
{
  &quot;query&quot;: {
    &quot;bool&quot;: {
      &quot;must&quot;:   [{ &quot;match&quot;: { &quot;title&quot;: &quot;入门&quot; } }],
      &quot;filter&quot;: [{ &quot;term&quot;:  { &quot;tags&quot;:  &quot;search&quot; } }]
    }
  },
  &quot;from&quot;: 0, &quot;size&quot;: 10,
  &quot;sort&quot;: [{ &quot;views&quot;: &quot;desc&quot; }]
}

# 聚合：按 tag 分桶并求平均浏览量
GET /articles/_search
{
  &quot;size&quot;: 0,
  &quot;aggs&quot;: {
    &quot;by_tag&quot;: {
      &quot;terms&quot;: { &quot;field&quot;: &quot;tags&quot; },
      &quot;aggs&quot;:  { &quot;avg_views&quot;: { &quot;avg&quot;: { &quot;field&quot;: &quot;views&quot; } } }
    }
  }
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>核心概念速记：</p><ul><li><strong>text vs keyword</strong>：<code>text</code> 会分词用于全文检索；<code>keyword</code> 不分词，用于精确匹配 / 聚合 / 排序。</li><li><strong>query vs filter</strong>：<code>query</code> 参与打分（相关性排序），<code>filter</code> 不打分但可缓存、更快。</li><li><strong>mapping</strong>：字段类型与分词器定义，写入前规划好可大幅提升质量。</li></ul><hr><h2 id="clickhouse" tabindex="-1"><a class="header-anchor" href="#clickhouse" aria-hidden="true">#</a> ClickHouse</h2><h3 id="简介-5" tabindex="-1"><a class="header-anchor" href="#简介-5" aria-hidden="true">#</a> 简介</h3><p>开源<strong>列式 OLAP 数据库</strong>（Online Analytical Processing），最初由 Yandex 开发并开源，现由 ClickHouse Inc. 维护。核心定位是<strong>对海量数据做聚合分析</strong>，在<strong>大范围扫描 + GROUP BY / 聚合</strong>场景下性能极强。特点：</p><ul><li><strong>列式存储 + 压缩</strong>：只读所需列，数据压缩比高、IO 少。</li><li><strong>向量化执行 + 多核并行</strong>：批量按列计算，吞吐高。</li><li><strong>MergeTree 家族引擎</strong>：按 <code>ORDER BY</code>（即主索引）排序落盘，按 <code>PARTITION BY</code> 分区，后台合并，适合按时间等维度的大表。</li><li><strong>SQL 方言</strong>：语法近似标准 SQL，并提供大量聚合 / 数组 / 时间 / 近似计算函数（如 <code>uniqExact</code>、<code>quantile</code>）。</li><li>原生<strong>分布式</strong>：通过 <code>Distributed</code> 表、分片与副本扩展到集群规模。</li></ul><h3 id="适用场景-5" tabindex="-1"><a class="header-anchor" href="#适用场景-5" aria-hidden="true">#</a> 适用场景</h3><ul><li><strong>日志 / 事件 / 埋点分析</strong>：每天亿级以上的事件数据，按时间 + 维度做 OLAP 查询。</li><li><strong>用户行为 / 漏斗 / 留存</strong>：大表上做复杂聚合、窗口、分位数计算。</li><li><strong>监控 / 可观测性后端</strong>：指标与 trace 聚合查询（如部分 APM、Sentry 后端即用 CH）。</li><li><strong>BI / 报表</strong>：替代传统数仓中 OLAP 层，对接 Superset、Metabase 等。</li><li><strong>数据湖加速层</strong>：作为 Kafka / 数据湖下游的高速查询层。</li></ul><p>不太适合：<strong>高并发小事务 OLTP</strong>（单点更新 / 删除不是强项，缺少成熟事务）；强一致跨行事务；频繁按主键单行点查的业务库。与 MySQL / PostgreSQL 是<strong>互补</strong>关系，常见架构是「MySQL 做业务 + CH 做分析」，中间用 CDC / 同步工具打通。</p><h3 id="语法要点-5" tabindex="-1"><a class="header-anchor" href="#语法要点-5" aria-hidden="true">#</a> 语法要点</h3><p>SQL 风格接近 MySQL / PostgreSQL，但建表时需要<strong>指定引擎</strong>，常用 <code>MergeTree</code>：</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token comment">-- 建一张按天分区、按 (user_id, ts) 排序的事件表</span>
<span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> events <span class="token punctuation">(</span>
  ts         <span class="token keyword">DateTime</span><span class="token punctuation">,</span>
  user_id    UInt64<span class="token punctuation">,</span>
  event      LowCardinality<span class="token punctuation">(</span>String<span class="token punctuation">)</span><span class="token punctuation">,</span>
  country    LowCardinality<span class="token punctuation">(</span>String<span class="token punctuation">)</span><span class="token punctuation">,</span>
  properties String
<span class="token punctuation">)</span>
<span class="token keyword">ENGINE</span> <span class="token operator">=</span> MergeTree
<span class="token keyword">PARTITION</span> <span class="token keyword">BY</span> toYYYYMMDD<span class="token punctuation">(</span>ts<span class="token punctuation">)</span>
<span class="token keyword">ORDER</span> <span class="token keyword">BY</span> <span class="token punctuation">(</span>user_id<span class="token punctuation">,</span> ts<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">-- 批量写入（CH 倾向于批量而非单行频繁 INSERT）</span>
<span class="token keyword">INSERT</span> <span class="token keyword">INTO</span> events <span class="token keyword">VALUES</span>
  <span class="token punctuation">(</span><span class="token string">&#39;2025-01-01 10:00:00&#39;</span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token string">&#39;click&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;CN&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;{}&#39;</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
  <span class="token punctuation">(</span><span class="token string">&#39;2025-01-01 10:00:05&#39;</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">,</span> <span class="token string">&#39;view&#39;</span><span class="token punctuation">,</span>  <span class="token string">&#39;US&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;{}&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">-- 典型聚合：每天各国 UV / PV</span>
<span class="token keyword">SELECT</span>
  toDate<span class="token punctuation">(</span>ts<span class="token punctuation">)</span>            <span class="token keyword">AS</span> <span class="token keyword">day</span><span class="token punctuation">,</span>
  country<span class="token punctuation">,</span>
  <span class="token function">count</span><span class="token punctuation">(</span><span class="token punctuation">)</span>               <span class="token keyword">AS</span> pv<span class="token punctuation">,</span>
  uniqExact<span class="token punctuation">(</span>user_id<span class="token punctuation">)</span>    <span class="token keyword">AS</span> uv
<span class="token keyword">FROM</span> events
<span class="token keyword">WHERE</span> ts <span class="token operator">&gt;=</span> <span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">-</span> <span class="token keyword">INTERVAL</span> <span class="token number">7</span> <span class="token keyword">DAY</span>
<span class="token keyword">GROUP</span> <span class="token keyword">BY</span> <span class="token keyword">day</span><span class="token punctuation">,</span> country
<span class="token keyword">ORDER</span> <span class="token keyword">BY</span> <span class="token keyword">day</span><span class="token punctuation">,</span> uv <span class="token keyword">DESC</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>实践要点：</p><ul><li>**排序键（ORDER BY）**就是主索引，决定查询过滤效率；选择最常用的过滤字段。</li><li>**分区（PARTITION BY）**常按时间（天 / 月），便于 TTL 清理和分区裁剪。</li><li><strong>批量写入</strong>优先：单行逐条 INSERT 会产生大量小 part，性能和稳定性都会变差。</li><li><strong>物化视图（Materialized View）</strong>：预聚合高频查询，显著降低线上查询成本。</li></ul>`,88),o=[i];function r(d,l){return s(),e("div",null,o)}const u=n(t,[["render",r],["__file","database.html.vue"]]);export{u as default};
